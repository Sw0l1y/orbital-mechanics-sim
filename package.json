{
  "name": "orbital-mechanics-sim",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:pages": "rm -rf docs && VITE_BASE_PATH=\"/$npm_package_name/\" npm run build && cp -R dist docs && touch docs/.nojekyll",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.0.10"
  }
}
